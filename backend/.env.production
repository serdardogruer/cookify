# Production Environment Variables
# VDS'ye deploy ederken bu dosyayı .env olarak kopyalayın

# Database (VDS PostgreSQL)
DATABASE_URL="postgresql://cookify_user:dgrr1213@localhost:5432/cookify"

# JWT (GÜVENLİ ŞİFRE OLUŞTURUN!)
JWT_SECRET="cookify-super-secret-jwt-key-2024-production"
JWT_EXPIRES_IN="7d"

# Server
PORT=5000
NODE_ENV="production"

# CORS (Frontend URL - IP ile, Domain transfer sonrası değişecek)
FRONTEND_URL="http://80.253.246.134:3000"

# File Upload
MAX_FILE_SIZE=5242880
UPLOAD_DIR="uploads"

# Google OAuth (Production credentials)
GOOGLE_CLIENT_ID="your-production-google-client-id.apps.googleusercontent.com"
